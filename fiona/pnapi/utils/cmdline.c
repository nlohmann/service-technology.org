/*
  File autogenerated by gengetopt version 2.22.2
  generated with the following command:
  gengetopt --input=./cmdline.ggo --set-version=4.02-unreleased --file-name=cmdline --unamed-opts=FILES --include-getopt --show-required --no-handle-error --conf-parser

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif


#include "cmdline.h"

const char *gengetopt_args_info_purpose = "A command-line frontend for the Petri Net API";

const char *gengetopt_args_info_usage = "Usage: petri [OPTIONS]... [FILES]...";

const char *gengetopt_args_info_description = "";

const char *gengetopt_args_info_detailed_help[] = {
  "  -h, --help                Print help and exit",
  "      --detailed-help       Print help, including all details and hidden \n                              options, and exit",
  "      --full-help           Print help, including hidden options, and exit",
  "  -V, --version             Print version and exit",
  "\nInput and Output:",
  "  -i, --input=FORMAT        Select the input file format  (possible \n                              values=\"owfn\", \"lola\", \"pnml\", \"sa\", \n                              \"tpn\" default=`owfn')",
  "  -o, --output=FORMAT       Select the output file format(s)  (possible \n                              values=\"owfn\", \"lola\", \"pnml\", \"sa\", \n                              \"tpn\", \"dot\", \"png\", \"pdf\", \"eps\", \n                              \"svg\")",
  "      --canonicalNames      Renames nodes in Woflan nets to avoid problems with \n                              conversion to other formats  (default=off)",
  "  -f, --formula             Places a final condition at the end of LoLA files. \n                              Has no effect to other output formats.  \n                              (default=off)",
  "      --guessFormula        Derive the final condition from the structure \n                              (i.e., the sink place) of the net.  (default=off)",
  "\n Group: Automaton Converter\n  Defining a program, to convert Service Automata to Petri Nets",
  "      --genet[=FILENAME]    Use Genet; specify the path  (default=`genet')",
  "      --petrify[=FILENAME]  Use Petrify; specify the path  (default=`petrify')",
  "\nComposition and Product:",
  "      --compose=NET         Compose each net given in the input section with \n                              this net.",
  "      --produce=CONSTRAINT  Calculate the product.",
  "\nCheck for Structural Properties:",
  "      --isFreeChoice        Check if the Petri net is free choice, i.e. if \n                              conflicting transitions share all pre-places.  \n                              (default=off)",
  "      --isNormal            Check if the Petri net is normal, i.e. every \n                              transition is connect to at most one interface \n                              place.  (default=off)",
  "      --isWorkflow          Check if the Petri net has workflow structure, i.e. \n                              it (i) has a distinct source place, (ii) has a \n                              district sink place, and (iii) every node lays on \n                              a path between the source place and the sink \n                              place.  (default=off)",
  "  -c, --check=PROPERTY      Check a structural property of the Petri net, see \n                              `isFreeChoice', `isNormal', and `isWorkflow' for \n                              details.  (possible values=\"freechoice\", \n                              \"normal\", \"workflow\")",
  "\nModifications:",
  "  -r, --reduce=RULES        Apply structural reduction rules, i.e. reduce the \n                              structure of the net while preserving liveness \n                              and boundedness.  (possible values=\"0\", \"1\", \n                              \"2\", \"3\", \"4\", \"5\", \"6\", \"starke\", \n                              \"pillat\", \"dead_nodes\", \"identical_places\", \n                              \"identical_transitions\", \"series_places\", \n                              \"series_transitions\", \"self_loop_places\", \n                              \"self_loop_transitions\", \"equal_places\", \n                              \"starke3p\", \"starke3t\", \"starke4\", \n                              \"starke5\", \"starke6\", \"starke7\", \n                              \"starke8\", \"starke9\", \"once\", \n                              \"k_boundedness\", \"boundedness\", \"liveness\")",
  "  -n, --normalize           Normalize the Petri net, i.e. change to structure \n                              such that every transition is connected to at \n                              most one interface place.  (default=off)",
  "  -N, --negate              Negate the final condition of the net, i.e. every \n                              specified final marking is now non-final. The \n                              result is the 'anti open net'.  (default=off)",
  "  -M, --mirror              Mirror the interface, i.e. change the direction of \n                              communication  (default=off)",
  "      --dnf                 Convert final condition to disjunctive normal form  \n                              (default=off)",
  "\nConfiguration:",
  "      --config=FILE         Read configuration from file.",
  "  If no file is given, Petri first tries to parse file `petri.conf' from the \n  sysconf directory (usually `/usr/local/etc'; written in case Petri was \n  installed) and then from the current working directory. If neither file was \n  found, the standard values are assumed.\n",
  "      --dot=FILENAME        Set the path an binary of dot.  (default=`dot')",
  "      --tmpfile=FILENAME    Set the path and name of temporary files  \n                              (default=`/tmp/petri-XXXXXX')",
  "  The filename Petri should use to create temporary files. The filename should \n  end with `XXXXXX' which is replaced at runtime with a random string.\n",
  "      --noClean             Do not delete temporary files.  (default=off)",
  "  Any temporary files created by Petri is kept after Petri terminates.\n",
  "\nDebugging:",
  "  -v, --verbose             Show verbose output  (default=off)",
  "      --suffix_owfn=SUFFIX  Suffix for open net files",
  "      --suffix_sa=SUFFIX    Suffix for service automaton files",
  "      --stats               Display time and memory consumption on termination. \n                               (default=off)",
  "  The time is measured using the function clock(3). The memory usage is \n  measured using the ps(1) command. Note that memory consumption contains a \n  small overhead.\n",
    0
};
static void
init_full_help_array(void)
{
  gengetopt_args_info_full_help[0] = gengetopt_args_info_detailed_help[0];
  gengetopt_args_info_full_help[1] = gengetopt_args_info_detailed_help[1];
  gengetopt_args_info_full_help[2] = gengetopt_args_info_detailed_help[2];
  gengetopt_args_info_full_help[3] = gengetopt_args_info_detailed_help[3];
  gengetopt_args_info_full_help[4] = gengetopt_args_info_detailed_help[4];
  gengetopt_args_info_full_help[5] = gengetopt_args_info_detailed_help[5];
  gengetopt_args_info_full_help[6] = gengetopt_args_info_detailed_help[6];
  gengetopt_args_info_full_help[7] = gengetopt_args_info_detailed_help[7];
  gengetopt_args_info_full_help[8] = gengetopt_args_info_detailed_help[8];
  gengetopt_args_info_full_help[9] = gengetopt_args_info_detailed_help[9];
  gengetopt_args_info_full_help[10] = gengetopt_args_info_detailed_help[10];
  gengetopt_args_info_full_help[11] = gengetopt_args_info_detailed_help[11];
  gengetopt_args_info_full_help[12] = gengetopt_args_info_detailed_help[12];
  gengetopt_args_info_full_help[13] = gengetopt_args_info_detailed_help[13];
  gengetopt_args_info_full_help[14] = gengetopt_args_info_detailed_help[14];
  gengetopt_args_info_full_help[15] = gengetopt_args_info_detailed_help[15];
  gengetopt_args_info_full_help[16] = gengetopt_args_info_detailed_help[16];
  gengetopt_args_info_full_help[17] = gengetopt_args_info_detailed_help[17];
  gengetopt_args_info_full_help[18] = gengetopt_args_info_detailed_help[18];
  gengetopt_args_info_full_help[19] = gengetopt_args_info_detailed_help[19];
  gengetopt_args_info_full_help[20] = gengetopt_args_info_detailed_help[20];
  gengetopt_args_info_full_help[21] = gengetopt_args_info_detailed_help[21];
  gengetopt_args_info_full_help[22] = gengetopt_args_info_detailed_help[22];
  gengetopt_args_info_full_help[23] = gengetopt_args_info_detailed_help[23];
  gengetopt_args_info_full_help[24] = gengetopt_args_info_detailed_help[24];
  gengetopt_args_info_full_help[25] = gengetopt_args_info_detailed_help[25];
  gengetopt_args_info_full_help[26] = gengetopt_args_info_detailed_help[26];
  gengetopt_args_info_full_help[27] = gengetopt_args_info_detailed_help[27];
  gengetopt_args_info_full_help[28] = gengetopt_args_info_detailed_help[28];
  gengetopt_args_info_full_help[29] = gengetopt_args_info_detailed_help[30];
  gengetopt_args_info_full_help[30] = gengetopt_args_info_detailed_help[31];
  gengetopt_args_info_full_help[31] = gengetopt_args_info_detailed_help[33];
  gengetopt_args_info_full_help[32] = gengetopt_args_info_detailed_help[35];
  gengetopt_args_info_full_help[33] = gengetopt_args_info_detailed_help[36];
  gengetopt_args_info_full_help[34] = gengetopt_args_info_detailed_help[37];
  gengetopt_args_info_full_help[35] = gengetopt_args_info_detailed_help[38];
  gengetopt_args_info_full_help[36] = gengetopt_args_info_detailed_help[39];
  gengetopt_args_info_full_help[37] = 0; 
  
}

const char *gengetopt_args_info_full_help[38];

static void
init_help_array(void)
{
  gengetopt_args_info_help[0] = gengetopt_args_info_detailed_help[0];
  gengetopt_args_info_help[1] = gengetopt_args_info_detailed_help[1];
  gengetopt_args_info_help[2] = gengetopt_args_info_detailed_help[2];
  gengetopt_args_info_help[3] = gengetopt_args_info_detailed_help[3];
  gengetopt_args_info_help[4] = gengetopt_args_info_detailed_help[4];
  gengetopt_args_info_help[5] = gengetopt_args_info_detailed_help[5];
  gengetopt_args_info_help[6] = gengetopt_args_info_detailed_help[6];
  gengetopt_args_info_help[7] = gengetopt_args_info_detailed_help[7];
  gengetopt_args_info_help[8] = gengetopt_args_info_detailed_help[8];
  gengetopt_args_info_help[9] = gengetopt_args_info_detailed_help[9];
  gengetopt_args_info_help[10] = gengetopt_args_info_detailed_help[10];
  gengetopt_args_info_help[11] = gengetopt_args_info_detailed_help[11];
  gengetopt_args_info_help[12] = gengetopt_args_info_detailed_help[12];
  gengetopt_args_info_help[13] = gengetopt_args_info_detailed_help[13];
  gengetopt_args_info_help[14] = gengetopt_args_info_detailed_help[14];
  gengetopt_args_info_help[15] = gengetopt_args_info_detailed_help[15];
  gengetopt_args_info_help[16] = gengetopt_args_info_detailed_help[16];
  gengetopt_args_info_help[17] = gengetopt_args_info_detailed_help[17];
  gengetopt_args_info_help[18] = gengetopt_args_info_detailed_help[18];
  gengetopt_args_info_help[19] = gengetopt_args_info_detailed_help[19];
  gengetopt_args_info_help[20] = gengetopt_args_info_detailed_help[21];
  gengetopt_args_info_help[21] = gengetopt_args_info_detailed_help[22];
  gengetopt_args_info_help[22] = gengetopt_args_info_detailed_help[23];
  gengetopt_args_info_help[23] = gengetopt_args_info_detailed_help[24];
  gengetopt_args_info_help[24] = gengetopt_args_info_detailed_help[25];
  gengetopt_args_info_help[25] = gengetopt_args_info_detailed_help[26];
  gengetopt_args_info_help[26] = gengetopt_args_info_detailed_help[27];
  gengetopt_args_info_help[27] = gengetopt_args_info_detailed_help[28];
  gengetopt_args_info_help[28] = gengetopt_args_info_detailed_help[33];
  gengetopt_args_info_help[29] = gengetopt_args_info_detailed_help[35];
  gengetopt_args_info_help[30] = gengetopt_args_info_detailed_help[36];
  gengetopt_args_info_help[31] = 0; 
  
}

const char *gengetopt_args_info_help[32];

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_ENUM
} cmdline_parser_arg_type;

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


const char *cmdline_parser_input_values[] = {"owfn", "lola", "pnml", "sa", "tpn", 0}; /*< Possible values for input. */
const char *cmdline_parser_output_values[] = {"owfn", "lola", "pnml", "sa", "tpn", "dot", "png", "pdf", "eps", "svg", 0}; /*< Possible values for output. */
const char *cmdline_parser_check_values[] = {"freechoice", "normal", "workflow", 0}; /*< Possible values for check. */
const char *cmdline_parser_reduce_values[] = {"0", "1", "2", "3", "4", "5", "6", "starke", "pillat", "dead_nodes", "identical_places", "identical_transitions", "series_places", "series_transitions", "self_loop_places", "self_loop_transitions", "equal_places", "starke3p", "starke3t", "starke4", "starke5", "starke6", "starke7", "starke8", "starke9", "once", "k_boundedness", "boundedness", "liveness", 0}; /*< Possible values for reduce. */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->detailed_help_given = 0 ;
  args_info->full_help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->input_given = 0 ;
  args_info->output_given = 0 ;
  args_info->canonicalNames_given = 0 ;
  args_info->formula_given = 0 ;
  args_info->guessFormula_given = 0 ;
  args_info->genet_given = 0 ;
  args_info->petrify_given = 0 ;
  args_info->compose_given = 0 ;
  args_info->produce_given = 0 ;
  args_info->isFreeChoice_given = 0 ;
  args_info->isNormal_given = 0 ;
  args_info->isWorkflow_given = 0 ;
  args_info->check_given = 0 ;
  args_info->reduce_given = 0 ;
  args_info->normalize_given = 0 ;
  args_info->negate_given = 0 ;
  args_info->mirror_given = 0 ;
  args_info->dnf_given = 0 ;
  args_info->config_given = 0 ;
  args_info->dot_given = 0 ;
  args_info->tmpfile_given = 0 ;
  args_info->noClean_given = 0 ;
  args_info->verbose_given = 0 ;
  args_info->suffix_owfn_given = 0 ;
  args_info->suffix_sa_given = 0 ;
  args_info->stats_given = 0 ;
  args_info->Automaton_Converter_group_counter = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->input_arg = input_arg_owfn;
  args_info->input_orig = NULL;
  args_info->output_arg = NULL;
  args_info->output_orig = NULL;
  args_info->canonicalNames_flag = 0;
  args_info->formula_flag = 0;
  args_info->guessFormula_flag = 0;
  args_info->genet_arg = gengetopt_strdup ("genet");
  args_info->genet_orig = NULL;
  args_info->petrify_arg = gengetopt_strdup ("petrify");
  args_info->petrify_orig = NULL;
  args_info->compose_arg = NULL;
  args_info->compose_orig = NULL;
  args_info->produce_arg = NULL;
  args_info->produce_orig = NULL;
  args_info->isFreeChoice_flag = 0;
  args_info->isNormal_flag = 0;
  args_info->isWorkflow_flag = 0;
  args_info->check_arg = -1;
  args_info->check_orig = NULL;
  args_info->reduce_arg = NULL;
  args_info->reduce_orig = NULL;
  args_info->normalize_flag = 0;
  args_info->negate_flag = 0;
  args_info->mirror_flag = 0;
  args_info->dnf_flag = 0;
  args_info->config_arg = NULL;
  args_info->config_orig = NULL;
  args_info->dot_arg = gengetopt_strdup ("dot");
  args_info->dot_orig = NULL;
  args_info->tmpfile_arg = gengetopt_strdup ("/tmp/petri-XXXXXX");
  args_info->tmpfile_orig = NULL;
  args_info->noClean_flag = 0;
  args_info->verbose_flag = 0;
  args_info->suffix_owfn_arg = NULL;
  args_info->suffix_owfn_orig = NULL;
  args_info->suffix_sa_arg = NULL;
  args_info->suffix_sa_orig = NULL;
  args_info->stats_flag = 0;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{
  init_full_help_array(); 
  init_help_array(); 
  args_info->help_help = gengetopt_args_info_detailed_help[0] ;
  args_info->detailed_help_help = gengetopt_args_info_detailed_help[1] ;
  args_info->full_help_help = gengetopt_args_info_detailed_help[2] ;
  args_info->version_help = gengetopt_args_info_detailed_help[3] ;
  args_info->input_help = gengetopt_args_info_detailed_help[5] ;
  args_info->output_help = gengetopt_args_info_detailed_help[6] ;
  args_info->output_min = 0;
  args_info->output_max = 0;
  args_info->canonicalNames_help = gengetopt_args_info_detailed_help[7] ;
  args_info->formula_help = gengetopt_args_info_detailed_help[8] ;
  args_info->guessFormula_help = gengetopt_args_info_detailed_help[9] ;
  args_info->genet_help = gengetopt_args_info_detailed_help[11] ;
  args_info->petrify_help = gengetopt_args_info_detailed_help[12] ;
  args_info->compose_help = gengetopt_args_info_detailed_help[14] ;
  args_info->produce_help = gengetopt_args_info_detailed_help[15] ;
  args_info->isFreeChoice_help = gengetopt_args_info_detailed_help[17] ;
  args_info->isNormal_help = gengetopt_args_info_detailed_help[18] ;
  args_info->isWorkflow_help = gengetopt_args_info_detailed_help[19] ;
  args_info->check_help = gengetopt_args_info_detailed_help[20] ;
  args_info->reduce_help = gengetopt_args_info_detailed_help[22] ;
  args_info->reduce_min = 0;
  args_info->reduce_max = 0;
  args_info->normalize_help = gengetopt_args_info_detailed_help[23] ;
  args_info->negate_help = gengetopt_args_info_detailed_help[24] ;
  args_info->mirror_help = gengetopt_args_info_detailed_help[25] ;
  args_info->dnf_help = gengetopt_args_info_detailed_help[26] ;
  args_info->config_help = gengetopt_args_info_detailed_help[28] ;
  args_info->dot_help = gengetopt_args_info_detailed_help[30] ;
  args_info->tmpfile_help = gengetopt_args_info_detailed_help[31] ;
  args_info->noClean_help = gengetopt_args_info_detailed_help[33] ;
  args_info->verbose_help = gengetopt_args_info_detailed_help[36] ;
  args_info->suffix_owfn_help = gengetopt_args_info_detailed_help[37] ;
  args_info->suffix_owfn_min = 0;
  args_info->suffix_owfn_max = 0;
  args_info->suffix_sa_help = gengetopt_args_info_detailed_help[38] ;
  args_info->suffix_sa_min = 0;
  args_info->suffix_sa_max = 0;
  args_info->stats_help = gengetopt_args_info_detailed_help[39] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n",
     (strlen(CMDLINE_PARSER_PACKAGE_NAME) ? CMDLINE_PARSER_PACKAGE_NAME : CMDLINE_PARSER_PACKAGE),
     CMDLINE_PARSER_VERSION);
}

static void print_help_common(void) {
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  if (strlen(gengetopt_args_info_usage) > 0)
    printf("\n%s\n", gengetopt_args_info_usage);

  printf("\n");

  if (strlen(gengetopt_args_info_description) > 0)
    printf("%s\n\n", gengetopt_args_info_description);
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_print_full_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_full_help[i])
    printf("%s\n", gengetopt_args_info_full_help[i++]);
}

void
cmdline_parser_print_detailed_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_detailed_help[i])
    printf("%s\n", gengetopt_args_info_detailed_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);

  args_info->inputs = 0;
  args_info->inputs_num = 0;
}

void
cmdline_parser_params_init(struct cmdline_parser_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct cmdline_parser_params *
cmdline_parser_params_create(void)
{
  struct cmdline_parser_params *params = 
    (struct cmdline_parser_params *)malloc(sizeof(struct cmdline_parser_params));
  cmdline_parser_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}

/** @brief generic value variable */
union generic_value {
    int int_arg;
    char *string_arg;
    const char *default_string_arg;
};

/** @brief holds temporary values for multiple options */
struct generic_list
{
  union generic_value arg;
  char *orig;
  struct generic_list *next;
};

/**
 * @brief add a node at the head of the list 
 */
static void add_node(struct generic_list **list) {
  struct generic_list *new_node = (struct generic_list *) malloc (sizeof (struct generic_list));
  new_node->next = *list;
  *list = new_node;
  new_node->arg.string_arg = 0;
  new_node->orig = 0;
}

/**
 * The passed arg parameter is NOT set to 0 from this function
 */
static void
free_multiple_field(unsigned int len, void *arg, char ***orig)
{
  unsigned int i;
  if (arg) {
    for (i = 0; i < len; ++i)
      {
        free_string_field(&((*orig)[i]));
      }

    free (arg);
    free (*orig);
    *orig = 0;
  }
}

static void
free_multiple_string_field(unsigned int len, char ***arg, char ***orig)
{
  unsigned int i;
  if (*arg) {
    for (i = 0; i < len; ++i)
      {
        free_string_field(&((*arg)[i]));
        free_string_field(&((*orig)[i]));
      }
    free_string_field(&((*arg)[0])); /* free default string */

    free (*arg);
    *arg = 0;
    free (*orig);
    *orig = 0;
  }
}

static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{
  unsigned int i;
  free_string_field (&(args_info->input_orig));
  free_multiple_field (args_info->output_given, (void *)(args_info->output_arg), &(args_info->output_orig));
  args_info->output_arg = 0;
  free_string_field (&(args_info->genet_arg));
  free_string_field (&(args_info->genet_orig));
  free_string_field (&(args_info->petrify_arg));
  free_string_field (&(args_info->petrify_orig));
  free_string_field (&(args_info->compose_arg));
  free_string_field (&(args_info->compose_orig));
  free_string_field (&(args_info->produce_arg));
  free_string_field (&(args_info->produce_orig));
  free_string_field (&(args_info->check_orig));
  free_multiple_field (args_info->reduce_given, (void *)(args_info->reduce_arg), &(args_info->reduce_orig));
  args_info->reduce_arg = 0;
  free_string_field (&(args_info->config_arg));
  free_string_field (&(args_info->config_orig));
  free_string_field (&(args_info->dot_arg));
  free_string_field (&(args_info->dot_orig));
  free_string_field (&(args_info->tmpfile_arg));
  free_string_field (&(args_info->tmpfile_orig));
  free_multiple_string_field (args_info->suffix_owfn_given, &(args_info->suffix_owfn_arg), &(args_info->suffix_owfn_orig));
  free_multiple_string_field (args_info->suffix_sa_given, &(args_info->suffix_sa_arg), &(args_info->suffix_sa_orig));
  
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);

  if (args_info->inputs_num)
    free (args_info->inputs);

  clear_given (args_info);
}

/**
 * @param val the value to check
 * @param values the possible values
 * @return the index of the matched value:
 * -1 if no value matched,
 * -2 if more than one value has matched
 */
static int
check_possible_values(const char *val, const char *values[])
{
  int i, found, last;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return -1; /* -1 means no argument for the option */

  found = last = 0;

  for (i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          ++found;
          last = i;
          if (strlen(values[i]) == len)
            return i; /* exact macth no need to check more */
        }
    }

  if (found == 1) /* one match: OK */
    return last;

  return (found ? -2 : -1); /* return many values or none matched */
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  int found = -1;
  if (arg) {
    if (values) {
      found = check_possible_values(arg, values);      
    }
    if (found >= 0)
      fprintf(outfile, "%s=\"%s\" # %s\n", opt, arg, values[found]);
    else
      fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}

static void
write_multiple_into_file(FILE *outfile, int len, const char *opt, char **arg, const char *values[])
{
  int i;
  
  for (i = 0; i < len; ++i)
    write_into_file(outfile, opt, (arg ? arg[i] : 0), values);
}

int
cmdline_parser_dump(FILE *outfile, struct gengetopt_args_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", CMDLINE_PARSER_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->detailed_help_given)
    write_into_file(outfile, "detailed-help", 0, 0 );
  if (args_info->full_help_given)
    write_into_file(outfile, "full-help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  if (args_info->input_given)
    write_into_file(outfile, "input", args_info->input_orig, cmdline_parser_input_values);
  write_multiple_into_file(outfile, args_info->output_given, "output", args_info->output_orig, cmdline_parser_output_values);
  if (args_info->canonicalNames_given)
    write_into_file(outfile, "canonicalNames", 0, 0 );
  if (args_info->formula_given)
    write_into_file(outfile, "formula", 0, 0 );
  if (args_info->guessFormula_given)
    write_into_file(outfile, "guessFormula", 0, 0 );
  if (args_info->genet_given)
    write_into_file(outfile, "genet", args_info->genet_orig, 0);
  if (args_info->petrify_given)
    write_into_file(outfile, "petrify", args_info->petrify_orig, 0);
  if (args_info->compose_given)
    write_into_file(outfile, "compose", args_info->compose_orig, 0);
  if (args_info->produce_given)
    write_into_file(outfile, "produce", args_info->produce_orig, 0);
  if (args_info->isFreeChoice_given)
    write_into_file(outfile, "isFreeChoice", 0, 0 );
  if (args_info->isNormal_given)
    write_into_file(outfile, "isNormal", 0, 0 );
  if (args_info->isWorkflow_given)
    write_into_file(outfile, "isWorkflow", 0, 0 );
  if (args_info->check_given)
    write_into_file(outfile, "check", args_info->check_orig, cmdline_parser_check_values);
  write_multiple_into_file(outfile, args_info->reduce_given, "reduce", args_info->reduce_orig, cmdline_parser_reduce_values);
  if (args_info->normalize_given)
    write_into_file(outfile, "normalize", 0, 0 );
  if (args_info->negate_given)
    write_into_file(outfile, "negate", 0, 0 );
  if (args_info->mirror_given)
    write_into_file(outfile, "mirror", 0, 0 );
  if (args_info->dnf_given)
    write_into_file(outfile, "dnf", 0, 0 );
  if (args_info->config_given)
    write_into_file(outfile, "config", args_info->config_orig, 0);
  if (args_info->dot_given)
    write_into_file(outfile, "dot", args_info->dot_orig, 0);
  if (args_info->tmpfile_given)
    write_into_file(outfile, "tmpfile", args_info->tmpfile_orig, 0);
  if (args_info->noClean_given)
    write_into_file(outfile, "noClean", 0, 0 );
  if (args_info->verbose_given)
    write_into_file(outfile, "verbose", 0, 0 );
  write_multiple_into_file(outfile, args_info->suffix_owfn_given, "suffix_owfn", args_info->suffix_owfn_orig, 0);
  write_multiple_into_file(outfile, args_info->suffix_sa_given, "suffix_sa", args_info->suffix_sa_orig, 0);
  if (args_info->stats_given)
    write_into_file(outfile, "stats", 0, 0 );
  

  i = EXIT_SUCCESS;
  return i;
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = cmdline_parser_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static char *
get_multiple_arg_token(const char *arg)
{
  char *tok, *ret;
  size_t len, num_of_escape, i, j;

  if (!arg)
    return 0;

  tok = strchr (arg, ',');
  num_of_escape = 0;

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
          ++num_of_escape;
        }
      else
        break;
    }

  if (tok)
    len = (size_t)(tok - arg + 1);
  else
    len = strlen (arg) + 1;

  len -= num_of_escape;

  ret = (char *) malloc (len);

  i = 0;
  j = 0;
  while (arg[i] && (j < len-1))
    {
      if (arg[i] == '\\' && 
	  arg[ i + 1 ] && 
	  arg[ i + 1 ] == ',')
        ++i;

      ret[j++] = arg[i++];
    }

  ret[len-1] = '\0';

  return ret;
}

static char *
get_multiple_arg_token_next(const char *arg)
{
  char *tok;

  if (!arg)
    return 0;

  tok = strchr (arg, ',');

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
        }
      else
        break;
    }

  if (! tok || strlen(tok) == 1)
    return 0;

  return tok+1;
}

static int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, unsigned int min, unsigned int max, const char *option_desc);

int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, unsigned int min, unsigned int max, const char *option_desc)
{
  int error = 0;

  if (option_given && (min > 0 || max > 0))
    {
      if (min > 0 && max > 0)
        {
          if (min == max)
            {
              /* specific occurrences */
              if (option_given != (unsigned int) min)
                {
                  fprintf (stderr, "%s: %s option occurrences must be %d\n",
                    prog_name, option_desc, min);
                  error = 1;
                }
            }
          else if (option_given < (unsigned int) min
                || option_given > (unsigned int) max)
            {
              /* range occurrences */
              fprintf (stderr, "%s: %s option occurrences must be between %d and %d\n",
                prog_name, option_desc, min, max);
              error = 1;
            }
        }
      else if (min > 0)
        {
          /* at least check */
          if (option_given < min)
            {
              fprintf (stderr, "%s: %s option occurrences must be at least %d\n",
                prog_name, option_desc, min);
              error = 1;
            }
        }
      else if (max > 0)
        {
          /* at most check */
          if (option_given > max)
            {
              fprintf (stderr, "%s: %s option occurrences must be at most %d\n",
                prog_name, option_desc, max);
              error = 1;
            }
        }
    }
    
  return error;
}
static void
reset_group_Automaton_Converter(struct gengetopt_args_info *args_info)
{
  if (! args_info->Automaton_Converter_group_counter)
    return;
  
  args_info->genet_given = 0 ;
  free_string_field (&(args_info->genet_arg));
  free_string_field (&(args_info->genet_orig));
  args_info->petrify_given = 0 ;
  free_string_field (&(args_info->petrify_arg));
  free_string_field (&(args_info->petrify_orig));

  args_info->Automaton_Converter_group_counter = 0;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser_ext (int argc, char * const *argv, struct gengetopt_args_info *args_info,
                   struct cmdline_parser_params *params)
{
  int result;
  result = cmdline_parser_internal (argc, argv, args_info, params, 0);

  return result;
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct cmdline_parser_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = cmdline_parser_internal (argc, argv, args_info, &params, 0);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  FIX_UNUSED (additional_error);
  int error = 0;

  /* checks for required options */
  if (check_multiple_option_occurrences(prog_name, args_info->output_given, args_info->output_min, args_info->output_max, "'--output' ('-o')"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->reduce_given, args_info->reduce_min, args_info->reduce_max, "'--reduce' ('-r')"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->suffix_owfn_given, args_info->suffix_owfn_min, args_info->suffix_owfn_max, "'--suffix_owfn'"))
     error = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->suffix_sa_given, args_info->suffix_sa_min, args_info->suffix_sa_max, "'--suffix_sa'"))
     error = 1;
  
  
  /* checks for dependences among options */

  return error;
}

/*
 * Extracted from the glibc source tree, version 2.3.6
 *
 * Licensed under the GPL as per the whole glibc source tree.
 *
 * This file was modified so that getopt_long can be called
 * many times without risking previous memory to be spoiled.
 *
 * Modified by Andre Noll and Lorenzo Bettini for use in
 * GNU gengetopt generated files.
 *
 */

/* 
 * we must include anything we need since this file is not thought to be
 * inserted in a file already using getopt.h
 *
 * Lorenzo
 */

struct option
{
  const char *name;
  /* has_arg can't be an enum because some compilers complain about
     type mismatches in all the code that assumes it is an int.  */
  int has_arg;
  int *flag;
  int val;
};

/* This version of `getopt' appears to the caller like standard Unix `getopt'
   but it behaves differently for the user, since it allows the user
   to intersperse the options with the other arguments.

   As `getopt' works, it permutes the elements of ARGV so that,
   when it is done, all the options precede everything else.  Thus
   all application programs are extended to handle flexible argument order.
*/
/*
   If the field `flag' is not NULL, it points to a variable that is set
   to the value given in the field `val' when the option is found, but
   left unchanged if the option is not found.

   To have a long-named option do something other than set an `int' to
   a compiled-in constant, such as set a value from `custom_optarg', set the
   option's `flag' field to zero and its `val' field to a nonzero
   value (the equivalent single-letter option character, if there is
   one).  For long options that have a zero `flag' field, `getopt'
   returns the contents of the `val' field.  */

/* Names for the values of the `has_arg' field of `struct option'.  */
#ifndef no_argument
#define no_argument		0
#endif

#ifndef required_argument
#define required_argument	1
#endif

#ifndef optional_argument
#define optional_argument	2
#endif

struct custom_getopt_data {
	/*
	 * These have exactly the same meaning as the corresponding global variables,
	 * except that they are used for the reentrant versions of getopt.
	 */
	int custom_optind;
	int custom_opterr;
	int custom_optopt;
	char *custom_optarg;

	/* True if the internal members have been initialized.  */
	int initialized;

	/*
	 * The next char to be scanned in the option-element in which the last option
	 * character we returned was found.  This allows us to pick up the scan where
	 * we left off.  If this is zero, or a null string, it means resume the scan by
	 * advancing to the next ARGV-element.
	 */
	char *nextchar;

	/*
	 * Describe the part of ARGV that contains non-options that have been skipped.
	 * `first_nonopt' is the index in ARGV of the first of them; `last_nonopt' is
	 * the index after the last of them.
	 */
	int first_nonopt;
	int last_nonopt;
};

/*
 * the variables optarg, optind, opterr and optopt are renamed with
 * the custom_ prefix so that they don't interfere with getopt ones.
 *
 * Moreover they're static so they are visible only from within the
 * file where this very file will be included.
 */

/*
 * For communication from `custom_getopt' to the caller.  When `custom_getopt' finds an
 * option that takes an argument, the argument value is returned here.
 */
static char *custom_optarg;

/*
 * Index in ARGV of the next element to be scanned.  This is used for
 * communication to and from the caller and for communication between
 * successive calls to `custom_getopt'.
 *
 * On entry to `custom_getopt', 1 means this is the first call; initialize.
 *
 * When `custom_getopt' returns -1, this is the index of the first of the non-option
 * elements that the caller should itself scan.
 *
 * Otherwise, `custom_optind' communicates from one call to the next how much of ARGV
 * has been scanned so far.
 *
 * 1003.2 says this must be 1 before any call.
 */
static int custom_optind = 1;

/*
 * Callers store zero here to inhibit the error message for unrecognized
 * options.
 */
static int custom_opterr = 1;

/*
 * Set to an option character which was unrecognized.  This must be initialized
 * on some systems to avoid linking in the system's own getopt implementation.
 */
static int custom_optopt = '?';

/*
 * Exchange two adjacent subsequences of ARGV.  One subsequence is elements
 * [first_nonopt,last_nonopt) which contains all the non-options that have been
 * skipped so far.  The other is elements [last_nonopt,custom_optind), which contains
 * all the options processed since those non-options were skipped.
 * `first_nonopt' and `last_nonopt' are relocated so that they describe the new
 * indices of the non-options in ARGV after they are moved.
 */
static void exchange(char **argv, struct custom_getopt_data *d)
{
	int bottom = d->first_nonopt;
	int middle = d->last_nonopt;
	int top = d->custom_optind;
	char *tem;

	/*
	 * Exchange the shorter segment with the far end of the longer segment.
	 * That puts the shorter segment into the right place.  It leaves the
	 * longer segment in the right place overall, but it consists of two
	 * parts that need to be swapped next.
	 */
	while (top > middle && middle > bottom) {
		if (top - middle > middle - bottom) {
			/* Bottom segment is the short one.  */
			int len = middle - bottom;
			int i;

			/* Swap it with the top part of the top segment.  */
			for (i = 0; i < len; i++) {
				tem = argv[bottom + i];
				argv[bottom + i] =
					argv[top - (middle - bottom) + i];
				argv[top - (middle - bottom) + i] = tem;
			}
			/* Exclude the moved bottom segment from further swapping.  */
			top -= len;
		} else {
			/* Top segment is the short one.  */
			int len = top - middle;
			int i;

			/* Swap it with the bottom part of the bottom segment.  */
			for (i = 0; i < len; i++) {
				tem = argv[bottom + i];
				argv[bottom + i] = argv[middle + i];
				argv[middle + i] = tem;
			}
			/* Exclude the moved top segment from further swapping.  */
			bottom += len;
		}
	}
	/* Update records for the slots the non-options now occupy.  */
	d->first_nonopt += (d->custom_optind - d->last_nonopt);
	d->last_nonopt = d->custom_optind;
}

/* Initialize the internal data when the first call is made.  */
static void custom_getopt_initialize(struct custom_getopt_data *d)
{
	/*
	 * Start processing options with ARGV-element 1 (since ARGV-element 0
	 * is the program name); the sequence of previously skipped non-option
	 * ARGV-elements is empty.
	 */
	d->first_nonopt = d->last_nonopt = d->custom_optind;
	d->nextchar = NULL;
	d->initialized = 1;
}

#define NONOPTION_P (argv[d->custom_optind][0] != '-' || argv[d->custom_optind][1] == '\0')

/* return: zero: continue, nonzero: return given value to user */
static int shuffle_argv(int argc, char *const *argv,const struct option *longopts,
	struct custom_getopt_data *d)
{
	/*
	 * Give FIRST_NONOPT & LAST_NONOPT rational values if CUSTOM_OPTIND has been
	 * moved back by the user (who may also have changed the arguments).
	 */
	if (d->last_nonopt > d->custom_optind)
		d->last_nonopt = d->custom_optind;
	if (d->first_nonopt > d->custom_optind)
		d->first_nonopt = d->custom_optind;
	/*
	 * If we have just processed some options following some
	 * non-options, exchange them so that the options come first.
	 */
	if (d->first_nonopt != d->last_nonopt &&
			d->last_nonopt != d->custom_optind)
		exchange((char **) argv, d);
	else if (d->last_nonopt != d->custom_optind)
		d->first_nonopt = d->custom_optind;
	/*
	 * Skip any additional non-options and extend the range of
	 * non-options previously skipped.
	 */
	while (d->custom_optind < argc && NONOPTION_P)
		d->custom_optind++;
	d->last_nonopt = d->custom_optind;
	/*
	 * The special ARGV-element `--' means premature end of options.  Skip
	 * it like a null option, then exchange with previous non-options as if
	 * it were an option, then skip everything else like a non-option.
	 */
	if (d->custom_optind != argc && !strcmp(argv[d->custom_optind], "--")) {
		d->custom_optind++;
		if (d->first_nonopt != d->last_nonopt
				&& d->last_nonopt != d->custom_optind)
			exchange((char **) argv, d);
		else if (d->first_nonopt == d->last_nonopt)
			d->first_nonopt = d->custom_optind;
		d->last_nonopt = argc;
		d->custom_optind = argc;
	}
	/*
	 * If we have done all the ARGV-elements, stop the scan and back over
	 * any non-options that we skipped and permuted.
	 */
	if (d->custom_optind == argc) {
		/*
		 * Set the next-arg-index to point at the non-options that we
		 * previously skipped, so the caller will digest them.
		 */
		if (d->first_nonopt != d->last_nonopt)
			d->custom_optind = d->first_nonopt;
		return -1;
	}
	/*
	 * If we have come to a non-option and did not permute it, either stop
	 * the scan or describe it to the caller and pass it by.
	 */
	if (NONOPTION_P) {
		d->custom_optarg = argv[d->custom_optind++];
		return 1;
	}
	/*
	 * We have found another option-ARGV-element. Skip the initial
	 * punctuation.
	 */
	d->nextchar = (argv[d->custom_optind] + 1 + (longopts != NULL && argv[d->custom_optind][1] == '-'));
	return 0;
}

/*
 * Check whether the ARGV-element is a long option.
 *
 * If there's a long option "fubar" and the ARGV-element is "-fu", consider
 * that an abbreviation of the long option, just like "--fu", and not "-f" with
 * arg "u".
 *
 * This distinction seems to be the most useful approach.
 *
 */
static int check_long_opt(int argc, char *const *argv, const char *optstring,
		const struct option *longopts, int *longind,
		int print_errors, struct custom_getopt_data *d)
{
	char *nameend;
	const struct option *p;
	const struct option *pfound = NULL;
	int exact = 0;
	int ambig = 0;
	int indfound = -1;
	int option_index;

	for (nameend = d->nextchar; *nameend && *nameend != '='; nameend++)
		/* Do nothing.  */ ;

	/* Test all long options for either exact match or abbreviated matches */
	for (p = longopts, option_index = 0; p->name; p++, option_index++)
		if (!strncmp(p->name, d->nextchar, nameend - d->nextchar)) {
			if ((unsigned int) (nameend - d->nextchar)
					== (unsigned int) strlen(p->name)) {
				/* Exact match found.  */
				pfound = p;
				indfound = option_index;
				exact = 1;
				break;
			} else if (pfound == NULL) {
				/* First nonexact match found.  */
				pfound = p;
				indfound = option_index;
			} else if (pfound->has_arg != p->has_arg
					|| pfound->flag != p->flag
					|| pfound->val != p->val)
				/* Second or later nonexact match found.  */
				ambig = 1;
		}
	if (ambig && !exact) {
		if (print_errors) {
			fprintf(stderr,
				"%s: option `%s' is ambiguous\n",
				argv[0], argv[d->custom_optind]);
		}
		d->nextchar += strlen(d->nextchar);
		d->custom_optind++;
		d->custom_optopt = 0;
		return '?';
	}
	if (pfound) {
		option_index = indfound;
		d->custom_optind++;
		if (*nameend) {
			if (pfound->has_arg != no_argument)
				d->custom_optarg = nameend + 1;
			else {
				if (print_errors) {
					if (argv[d->custom_optind - 1][1] == '-') {
						/* --option */
						fprintf(stderr, "%s: option `--%s' doesn't allow an argument\n",
							argv[0], pfound->name);
					} else {
						/* +option or -option */
						fprintf(stderr, "%s: option `%c%s' doesn't allow an argument\n",
							argv[0], argv[d->custom_optind - 1][0], pfound->name);
					}

				}
				d->nextchar += strlen(d->nextchar);
				d->custom_optopt = pfound->val;
				return '?';
			}
		} else if (pfound->has_arg == required_argument) {
			if (d->custom_optind < argc)
				d->custom_optarg = argv[d->custom_optind++];
			else {
				if (print_errors) {
					fprintf(stderr,
						"%s: option `%s' requires an argument\n",
						argv[0],
						argv[d->custom_optind - 1]);
				}
				d->nextchar += strlen(d->nextchar);
				d->custom_optopt = pfound->val;
				return optstring[0] == ':' ? ':' : '?';
			}
		}
		d->nextchar += strlen(d->nextchar);
		if (longind != NULL)
			*longind = option_index;
		if (pfound->flag) {
			*(pfound->flag) = pfound->val;
			return 0;
		}
		return pfound->val;
	}
	/*
	 * Can't find it as a long option.  If this is not getopt_long_only, or
	 * the option starts with '--' or is not a valid short option, then
	 * it's an error.  Otherwise interpret it as a short option.
	 */
	if (print_errors) {
		if (argv[d->custom_optind][1] == '-') {
			/* --option */
			fprintf(stderr,
				"%s: unrecognized option `--%s'\n",
				argv[0], d->nextchar);
		} else {
			/* +option or -option */
			fprintf(stderr,
				"%s: unrecognized option `%c%s'\n",
				argv[0], argv[d->custom_optind][0],
				d->nextchar);
		}
	}
	d->nextchar = (char *) "";
	d->custom_optind++;
	d->custom_optopt = 0;
	return '?';
}

static int check_short_opt(int argc, char *const *argv, const char *optstring,
		int print_errors, struct custom_getopt_data *d)
{
	char c = *d->nextchar++;
	const char *temp = strchr(optstring, c);

	/* Increment `custom_optind' when we start to process its last character.  */
	if (*d->nextchar == '\0')
		++d->custom_optind;
	if (!temp || c == ':') {
		if (print_errors)
			fprintf(stderr, "%s: invalid option -- %c\n", argv[0], c);

		d->custom_optopt = c;
		return '?';
	}
	if (temp[1] == ':') {
		if (temp[2] == ':') {
			/* This is an option that accepts an argument optionally.  */
			if (*d->nextchar != '\0') {
				d->custom_optarg = d->nextchar;
				d->custom_optind++;
			} else
				d->custom_optarg = NULL;
			d->nextchar = NULL;
		} else {
			/* This is an option that requires an argument.  */
			if (*d->nextchar != '\0') {
				d->custom_optarg = d->nextchar;
				/*
				 * If we end this ARGV-element by taking the
				 * rest as an arg, we must advance to the next
				 * element now.
				 */
				d->custom_optind++;
			} else if (d->custom_optind == argc) {
				if (print_errors) {
					fprintf(stderr,
						"%s: option requires an argument -- %c\n",
						argv[0], c);
				}
				d->custom_optopt = c;
				if (optstring[0] == ':')
					c = ':';
				else
					c = '?';
			} else
				/*
				 * We already incremented `custom_optind' once;
				 * increment it again when taking next ARGV-elt
				 * as argument.
				 */
				d->custom_optarg = argv[d->custom_optind++];
			d->nextchar = NULL;
		}
	}
	return c;
}

/*
 * Scan elements of ARGV for option characters given in OPTSTRING.
 *
 * If an element of ARGV starts with '-', and is not exactly "-" or "--",
 * then it is an option element.  The characters of this element
 * (aside from the initial '-') are option characters.  If `getopt'
 * is called repeatedly, it returns successively each of the option characters
 * from each of the option elements.
 *
 * If `getopt' finds another option character, it returns that character,
 * updating `custom_optind' and `nextchar' so that the next call to `getopt' can
 * resume the scan with the following option character or ARGV-element.
 *
 * If there are no more option characters, `getopt' returns -1.
 * Then `custom_optind' is the index in ARGV of the first ARGV-element
 * that is not an option.  (The ARGV-elements have been permuted
 * so that those that are not options now come last.)
 *
 * OPTSTRING is a string containing the legitimate option characters.
 * If an option character is seen that is not listed in OPTSTRING,
 * return '?' after printing an error message.  If you set `custom_opterr' to
 * zero, the error message is suppressed but we still return '?'.
 *
 * If a char in OPTSTRING is followed by a colon, that means it wants an arg,
 * so the following text in the same ARGV-element, or the text of the following
 * ARGV-element, is returned in `custom_optarg'.  Two colons mean an option that
 * wants an optional arg; if there is text in the current ARGV-element,
 * it is returned in `custom_optarg', otherwise `custom_optarg' is set to zero.
 *
 * If OPTSTRING starts with `-' or `+', it requests different methods of
 * handling the non-option ARGV-elements.
 * See the comments about RETURN_IN_ORDER and REQUIRE_ORDER, above.
 *
 * Long-named options begin with `--' instead of `-'.
 * Their names may be abbreviated as long as the abbreviation is unique
 * or is an exact match for some defined option.  If they have an
 * argument, it follows the option name in the same ARGV-element, separated
 * from the option name by a `=', or else the in next ARGV-element.
 * When `getopt' finds a long-named option, it returns 0 if that option's
 * `flag' field is nonzero, the value of the option's `val' field
 * if the `flag' field is zero.
 *
 * The elements of ARGV aren't really const, because we permute them.
 * But we pretend they're const in the prototype to be compatible
 * with other systems.
 *
 * LONGOPTS is a vector of `struct option' terminated by an
 * element containing a name which is zero.
 *
 * LONGIND returns the index in LONGOPT of the long-named option found.
 * It is only valid when a long-named option has been found by the most
 * recent call.
 *
 * Return the option character from OPTS just read.  Return -1 when there are
 * no more options.  For unrecognized options, or options missing arguments,
 * `custom_optopt' is set to the option letter, and '?' is returned.
 *
 * The OPTS string is a list of characters which are recognized option letters,
 * optionally followed by colons, specifying that that letter takes an
 * argument, to be placed in `custom_optarg'.
 *
 * If a letter in OPTS is followed by two colons, its argument is optional.
 * This behavior is specific to the GNU `getopt'.
 *
 * The argument `--' causes premature termination of argument scanning,
 * explicitly telling `getopt' that there are no more options.  If OPTS begins
 * with `--', then non-option arguments are treated as arguments to the option
 * '\0'.  This behavior is specific to the GNU `getopt'.
 */

static int getopt_internal_r(int argc, char *const *argv, const char *optstring,
		const struct option *longopts, int *longind,
		struct custom_getopt_data *d)
{
	int ret, print_errors = d->custom_opterr;

	if (optstring[0] == ':')
		print_errors = 0;
	if (argc < 1)
		return -1;
	d->custom_optarg = NULL;

	/* 
	 * This is a big difference with GNU getopt, since optind == 0
	 * means initialization while here 1 means first call.
	 */
	if (d->custom_optind == 0 || !d->initialized) {
		if (d->custom_optind == 0)
			d->custom_optind = 1;	/* Don't scan ARGV[0], the program name.  */
		custom_getopt_initialize(d);
	}
	if (d->nextchar == NULL || *d->nextchar == '\0') {
		ret = shuffle_argv(argc, argv, longopts, d);
		if (ret)
			return ret;
	}
	if (longopts && (argv[d->custom_optind][1] == '-' ))
		return check_long_opt(argc, argv, optstring, longopts,
			longind, print_errors, d);
	return check_short_opt(argc, argv, optstring, print_errors, d);
}

static int custom_getopt_internal(int argc, char *const *argv, const char *optstring,
	const struct option *longopts, int *longind)
{
	int result;
	/* Keep a global copy of all internal members of d */
	static struct custom_getopt_data d;

	d.custom_optind = custom_optind;
	d.custom_opterr = custom_opterr;
	result = getopt_internal_r(argc, argv, optstring, longopts,
		longind, &d);
	custom_optind = d.custom_optind;
	custom_optarg = d.custom_optarg;
	custom_optopt = d.custom_optopt;
	return result;
}

static int custom_getopt_long (int argc, char *const *argv, const char *options,
	const struct option *long_options, int *opt_index)
{
	return custom_getopt_internal(argc, argv, options, long_options,
		opt_index);
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see cmdline_parser_params.check_ambiguity
 * @param override @see cmdline_parser_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               cmdline_parser_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  FIX_UNUSED (field);
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  if (possible_values && (found = check_possible_values((value ? value : default_value), possible_values)) < 0)
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s' (`-%c')%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt, short_opt,
          (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s'%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt,
          (additional_error ? additional_error : ""));
      return 1; /* failure */
    }
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_ENUM:
    if (val) *((int *)field) = found;
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };


  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}

/**
 * @brief store information about a multiple option in a temporary list
 * @param list where to (temporarily) store multiple options
 */
static
int update_multiple_arg_temp(struct generic_list **list,
               unsigned int *prev_given, const char *val,
               const char *possible_values[], const char *default_value,
               cmdline_parser_arg_type arg_type,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *multi_token, *multi_next; /* store single arguments */

  if (arg_type == ARG_NO) {
    (*prev_given)++;
    return 0; /* OK */
  }

  multi_token = get_multiple_arg_token(val);
  multi_next = get_multiple_arg_token_next (val);

  while (1)
    {
      add_node (list);
      if (update_arg((void *)&((*list)->arg), &((*list)->orig), 0,
          prev_given, multi_token, possible_values, default_value, 
          arg_type, 0, 1, 1, 1, long_opt, short_opt, additional_error)) {
        if (multi_token) free(multi_token);
        return 1; /* failure */
      }

      if (multi_next)
        {
          multi_token = get_multiple_arg_token(multi_next);
          multi_next = get_multiple_arg_token_next (multi_next);
        }
      else
        break;
    }

  return 0; /* OK */
}

/**
 * @brief free the passed list (including possible string argument)
 */
static
void free_list(struct generic_list *list, short string_arg)
{
  if (list) {
    struct generic_list *tmp;
    while (list)
      {
        tmp = list;
        if (string_arg && list->arg.string_arg)
          free (list->arg.string_arg);
        if (list->orig)
          free (list->orig);
        list = list->next;
        free (tmp);
      }
  }
}

/**
 * @brief updates a multiple option starting from the passed list
 */
static
void update_multiple_arg(void *field, char ***orig_field,
               unsigned int field_given, unsigned int prev_given, union generic_value *default_value,
               cmdline_parser_arg_type arg_type,
               struct generic_list *list)
{
  int i;
  struct generic_list *tmp;

  if (prev_given && list) {
    *orig_field = (char **) realloc (*orig_field, (field_given + prev_given) * sizeof (char *));

    switch(arg_type) {
    case ARG_ENUM:
      *((int **)field) = (int *)realloc (*((int **)field), (field_given + prev_given) * sizeof (int)); break;
    case ARG_STRING:
      *((char ***)field) = (char **)realloc (*((char ***)field), (field_given + prev_given) * sizeof (char *)); break;
    default:
      break;
    };
    
    for (i = (prev_given - 1); i >= 0; --i)
      {
        tmp = list;
        
        switch(arg_type) {
        case ARG_ENUM:
          (*((int **)field))[i + field_given] = tmp->arg.int_arg; break;
        case ARG_STRING:
          (*((char ***)field))[i + field_given] = tmp->arg.string_arg; break;
        default:
          break;
        }        
        (*orig_field) [i + field_given] = list->orig;
        list = list->next;
        free (tmp);
      }
  } else { /* set the default value */
    if (default_value && ! field_given) {
      switch(arg_type) {
      case ARG_ENUM:
        if (! *((int **)field)) {
          *((int **)field) = (int *)malloc (sizeof (int));
          (*((int **)field))[0] = default_value->int_arg; 
        }
        break;
      case ARG_STRING:
        if (! *((char ***)field)) {
          *((char ***)field) = (char **)malloc (sizeof (char *));
          (*((char ***)field))[0] = gengetopt_strdup(default_value->string_arg);
        }
        break;
      default: break;
      }
      if (!(*orig_field)) {
        *orig_field = (char **) malloc (sizeof (char *));
        (*orig_field)[0] = 0;
      }
    }
  }
}

int
cmdline_parser_internal (
  int argc, char * const *argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  struct generic_list * output_list = NULL;
  struct generic_list * reduce_list = NULL;
  struct generic_list * suffix_owfn_list = NULL;
  struct generic_list * suffix_sa_list = NULL;
  int error = 0;
  struct gengetopt_args_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;

  char *optarg;
  int optind;
  int opterr;
  int optopt;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "detailed-help",	0, NULL, 0 },
        { "full-help",	0, NULL, 0 },
        { "version",	0, NULL, 'V' },
        { "input",	1, NULL, 'i' },
        { "output",	1, NULL, 'o' },
        { "canonicalNames",	0, NULL, 0 },
        { "formula",	0, NULL, 'f' },
        { "guessFormula",	0, NULL, 0 },
        { "genet",	2, NULL, 0 },
        { "petrify",	2, NULL, 0 },
        { "compose",	1, NULL, 0 },
        { "produce",	1, NULL, 0 },
        { "isFreeChoice",	0, NULL, 0 },
        { "isNormal",	0, NULL, 0 },
        { "isWorkflow",	0, NULL, 0 },
        { "check",	1, NULL, 'c' },
        { "reduce",	1, NULL, 'r' },
        { "normalize",	0, NULL, 'n' },
        { "negate",	0, NULL, 'N' },
        { "mirror",	0, NULL, 'M' },
        { "dnf",	0, NULL, 0 },
        { "config",	1, NULL, 0 },
        { "dot",	1, NULL, 0 },
        { "tmpfile",	1, NULL, 0 },
        { "noClean",	0, NULL, 0 },
        { "verbose",	0, NULL, 'v' },
        { "suffix_owfn",	1, NULL, 0 },
        { "suffix_sa",	1, NULL, 0 },
        { "stats",	0, NULL, 0 },
        { 0,  0, 0, 0 }
      };

      custom_optarg = optarg;
      custom_optind = optind;
      custom_opterr = opterr;
      custom_optopt = optopt;

      c = custom_getopt_long (argc, argv, "hVi:o:fc:r:nNMv", long_options, &option_index);

      optarg = custom_optarg;
      optind = custom_optind;
      opterr = custom_opterr;
      optopt = custom_optopt;

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          cmdline_parser_print_version ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'i':	/* Select the input file format.  */
        
        
          if (update_arg( (void *)&(args_info->input_arg), 
               &(args_info->input_orig), &(args_info->input_given),
              &(local_args_info.input_given), optarg, cmdline_parser_input_values, "owfn", ARG_ENUM,
              check_ambiguity, override, 0, 0,
              "input", 'i',
              additional_error))
            goto failure;
        
          break;
        case 'o':	/* Select the output file format(s).  */
        
          if (update_multiple_arg_temp(&output_list, 
              &(local_args_info.output_given), optarg, cmdline_parser_output_values, 0, ARG_ENUM,
              "output", 'o',
              additional_error))
            goto failure;
        
          break;
        case 'f':	/* Places a final condition at the end of LoLA files. Has no effect to other output formats..  */
        
        
          if (update_arg((void *)&(args_info->formula_flag), 0, &(args_info->formula_given),
              &(local_args_info.formula_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "formula", 'f',
              additional_error))
            goto failure;
        
          break;
        case 'c':	/* Check a structural property of the Petri net, see `isFreeChoice', `isNormal', and `isWorkflow' for details..  */
        
        
          if (update_arg( (void *)&(args_info->check_arg), 
               &(args_info->check_orig), &(args_info->check_given),
              &(local_args_info.check_given), optarg, cmdline_parser_check_values, 0, ARG_ENUM,
              check_ambiguity, override, 0, 0,
              "check", 'c',
              additional_error))
            goto failure;
        
          break;
        case 'r':	/* Apply structural reduction rules, i.e. reduce the structure of the net while preserving liveness and boundedness..  */
        
          if (update_multiple_arg_temp(&reduce_list, 
              &(local_args_info.reduce_given), optarg, cmdline_parser_reduce_values, 0, ARG_ENUM,
              "reduce", 'r',
              additional_error))
            goto failure;
        
          break;
        case 'n':	/* Normalize the Petri net, i.e. change to structure such that every transition is connected to at most one interface place..  */
        
        
          if (update_arg((void *)&(args_info->normalize_flag), 0, &(args_info->normalize_given),
              &(local_args_info.normalize_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "normalize", 'n',
              additional_error))
            goto failure;
        
          break;
        case 'N':	/* Negate the final condition of the net, i.e. every specified final marking is now non-final. The result is the 'anti open net'..  */
        
        
          if (update_arg((void *)&(args_info->negate_flag), 0, &(args_info->negate_given),
              &(local_args_info.negate_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "negate", 'N',
              additional_error))
            goto failure;
        
          break;
        case 'M':	/* Mirror the interface, i.e. change the direction of communication.  */
        
        
          if (update_arg((void *)&(args_info->mirror_flag), 0, &(args_info->mirror_given),
              &(local_args_info.mirror_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "mirror", 'M',
              additional_error))
            goto failure;
        
          break;
        case 'v':	/* Show verbose output.  */
        
        
          if (update_arg((void *)&(args_info->verbose_flag), 0, &(args_info->verbose_given),
              &(local_args_info.verbose_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "verbose", 'v',
              additional_error))
            goto failure;
        
          break;

        case 0:	/* Long option with no short option */
          if (strcmp (long_options[option_index].name, "detailed-help") == 0) {
            cmdline_parser_print_detailed_help ();
            cmdline_parser_free (&local_args_info);
            exit (EXIT_SUCCESS);
          }

          if (strcmp (long_options[option_index].name, "full-help") == 0) {
            cmdline_parser_print_full_help ();
            cmdline_parser_free (&local_args_info);
            exit (EXIT_SUCCESS);
          }

          /* Renames nodes in Woflan nets to avoid problems with conversion to other formats.  */
          if (strcmp (long_options[option_index].name, "canonicalNames") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->canonicalNames_flag), 0, &(args_info->canonicalNames_given),
                &(local_args_info.canonicalNames_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "canonicalNames", '-',
                additional_error))
              goto failure;
          
          }
          /* Derive the final condition from the structure (i.e., the sink place) of the net..  */
          else if (strcmp (long_options[option_index].name, "guessFormula") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->guessFormula_flag), 0, &(args_info->guessFormula_given),
                &(local_args_info.guessFormula_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "guessFormula", '-',
                additional_error))
              goto failure;
          
          }
          /* Use Genet; specify the path.  */
          else if (strcmp (long_options[option_index].name, "genet") == 0)
          {
          
            if (args_info->Automaton_Converter_group_counter && override)
              reset_group_Automaton_Converter (args_info);
            args_info->Automaton_Converter_group_counter += 1;
          
            if (update_arg( (void *)&(args_info->genet_arg), 
                 &(args_info->genet_orig), &(args_info->genet_given),
                &(local_args_info.genet_given), optarg, 0, "genet", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "genet", '-',
                additional_error))
              goto failure;
          
          }
          /* Use Petrify; specify the path.  */
          else if (strcmp (long_options[option_index].name, "petrify") == 0)
          {
          
            if (args_info->Automaton_Converter_group_counter && override)
              reset_group_Automaton_Converter (args_info);
            args_info->Automaton_Converter_group_counter += 1;
          
            if (update_arg( (void *)&(args_info->petrify_arg), 
                 &(args_info->petrify_orig), &(args_info->petrify_given),
                &(local_args_info.petrify_given), optarg, 0, "petrify", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "petrify", '-',
                additional_error))
              goto failure;
          
          }
          /* Compose each net given in the input section with this net..  */
          else if (strcmp (long_options[option_index].name, "compose") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->compose_arg), 
                 &(args_info->compose_orig), &(args_info->compose_given),
                &(local_args_info.compose_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "compose", '-',
                additional_error))
              goto failure;
          
          }
          /* Calculate the product..  */
          else if (strcmp (long_options[option_index].name, "produce") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->produce_arg), 
                 &(args_info->produce_orig), &(args_info->produce_given),
                &(local_args_info.produce_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "produce", '-',
                additional_error))
              goto failure;
          
          }
          /* Check if the Petri net is free choice, i.e. if conflicting transitions share all pre-places..  */
          else if (strcmp (long_options[option_index].name, "isFreeChoice") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->isFreeChoice_flag), 0, &(args_info->isFreeChoice_given),
                &(local_args_info.isFreeChoice_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "isFreeChoice", '-',
                additional_error))
              goto failure;
          
          }
          /* Check if the Petri net is normal, i.e. every transition is connect to at most one interface place..  */
          else if (strcmp (long_options[option_index].name, "isNormal") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->isNormal_flag), 0, &(args_info->isNormal_given),
                &(local_args_info.isNormal_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "isNormal", '-',
                additional_error))
              goto failure;
          
          }
          /* Check if the Petri net has workflow structure, i.e. it (i) has a distinct source place, (ii) has a district sink place, and (iii) every node lays on a path between the source place and the sink place..  */
          else if (strcmp (long_options[option_index].name, "isWorkflow") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->isWorkflow_flag), 0, &(args_info->isWorkflow_given),
                &(local_args_info.isWorkflow_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "isWorkflow", '-',
                additional_error))
              goto failure;
          
          }
          /* Convert final condition to disjunctive normal form.  */
          else if (strcmp (long_options[option_index].name, "dnf") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->dnf_flag), 0, &(args_info->dnf_given),
                &(local_args_info.dnf_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "dnf", '-',
                additional_error))
              goto failure;
          
          }
          /* Read configuration from file..  */
          else if (strcmp (long_options[option_index].name, "config") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->config_arg), 
                 &(args_info->config_orig), &(args_info->config_given),
                &(local_args_info.config_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "config", '-',
                additional_error))
              goto failure;
          
          }
          /* Set the path an binary of dot..  */
          else if (strcmp (long_options[option_index].name, "dot") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dot_arg), 
                 &(args_info->dot_orig), &(args_info->dot_given),
                &(local_args_info.dot_given), optarg, 0, "dot", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "dot", '-',
                additional_error))
              goto failure;
          
          }
          /* Set the path and name of temporary files.  */
          else if (strcmp (long_options[option_index].name, "tmpfile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->tmpfile_arg), 
                 &(args_info->tmpfile_orig), &(args_info->tmpfile_given),
                &(local_args_info.tmpfile_given), optarg, 0, "/tmp/petri-XXXXXX", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "tmpfile", '-',
                additional_error))
              goto failure;
          
          }
          /* Do not delete temporary files..  */
          else if (strcmp (long_options[option_index].name, "noClean") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->noClean_flag), 0, &(args_info->noClean_given),
                &(local_args_info.noClean_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "noClean", '-',
                additional_error))
              goto failure;
          
          }
          /* Suffix for open net files.  */
          else if (strcmp (long_options[option_index].name, "suffix_owfn") == 0)
          {
          
            if (update_multiple_arg_temp(&suffix_owfn_list, 
                &(local_args_info.suffix_owfn_given), optarg, 0, 0, ARG_STRING,
                "suffix_owfn", '-',
                additional_error))
              goto failure;
          
          }
          /* Suffix for service automaton files.  */
          else if (strcmp (long_options[option_index].name, "suffix_sa") == 0)
          {
          
            if (update_multiple_arg_temp(&suffix_sa_list, 
                &(local_args_info.suffix_sa_given), optarg, 0, 0, ARG_STRING,
                "suffix_sa", '-',
                additional_error))
              goto failure;
          
          }
          /* Display time and memory consumption on termination..  */
          else if (strcmp (long_options[option_index].name, "stats") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->stats_flag), 0, &(args_info->stats_given),
                &(local_args_info.stats_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "stats", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */

  if (args_info->Automaton_Converter_group_counter > 1)
    {
      fprintf (stderr, "%s: %d options of group Automaton Converter were given. At most one is required%s.\n", argv[0], args_info->Automaton_Converter_group_counter, (additional_error ? additional_error : ""));
      error = 1;
    }
  

  update_multiple_arg((void *)&(args_info->output_arg),
    &(args_info->output_orig), args_info->output_given,
    local_args_info.output_given, 0,
    ARG_ENUM, output_list);
  update_multiple_arg((void *)&(args_info->reduce_arg),
    &(args_info->reduce_orig), args_info->reduce_given,
    local_args_info.reduce_given, 0,
    ARG_ENUM, reduce_list);
  update_multiple_arg((void *)&(args_info->suffix_owfn_arg),
    &(args_info->suffix_owfn_orig), args_info->suffix_owfn_given,
    local_args_info.suffix_owfn_given, 0,
    ARG_STRING, suffix_owfn_list);
  update_multiple_arg((void *)&(args_info->suffix_sa_arg),
    &(args_info->suffix_sa_orig), args_info->suffix_sa_given,
    local_args_info.suffix_sa_given, 0,
    ARG_STRING, suffix_sa_list);

  args_info->output_given += local_args_info.output_given;
  local_args_info.output_given = 0;
  args_info->reduce_given += local_args_info.reduce_given;
  local_args_info.reduce_given = 0;
  args_info->suffix_owfn_given += local_args_info.suffix_owfn_given;
  local_args_info.suffix_owfn_given = 0;
  args_info->suffix_sa_given += local_args_info.suffix_sa_given;
  local_args_info.suffix_sa_given = 0;
  
  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;
      int found_prog_name = 0;
      /* whether program name, i.e., argv[0], is in the remaining args
         (this may happen with some implementations of getopt,
          but surely not with the one included by gengetopt) */


      args_info->inputs_num = argc - optind - found_prog_name;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind++]) ;
    }

  return 0;

failure:
  free_list (output_list, 0 );
  free_list (reduce_list, 0 );
  free_list (suffix_owfn_list, 1 );
  free_list (suffix_sa_list, 1 );
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

static int
_cmdline_parser_configfile (const char *filename, int *my_argc)
{
  FILE* file;
  char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int result = 0, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;

  if ((file = fopen(filename, "r")) == 0)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != 0)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          break;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn (linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = 0;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");

      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }

    noarg:
      if (!strcmp(fopt,"include")) {
        if (farg && *farg) {
          result = _cmdline_parser_configfile(farg, my_argc);
        } else {
          fprintf(stderr, "%s:%s:%d: include requires a filename argument.\n",
                  CMDLINE_PARSER_PACKAGE, filename, line_num);
        }
        continue;
      }
      len = strlen(fopt);
      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg && *farg) || equal))
        strcat (my_argv, "=");
      if (farg && *farg)
        strcat (my_argv, farg);
      ++(*my_argc);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  if (file)
    fclose(file);
  return result;
}

int
cmdline_parser_configfile (
  const char *filename,
                           struct gengetopt_args_info *args_info,
                           int override, int initialize, int check_required)
{
  struct cmdline_parser_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;
  
  return cmdline_parser_config_file (filename, args_info, &params);
}

int
cmdline_parser_config_file (const char *filename,
                           struct gengetopt_args_info *args_info,
                           struct cmdline_parser_params *params)
{
  int i, result;
  int my_argc = 1;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (CMDLINE_PARSER_PACKAGE);

  result = _cmdline_parser_configfile(filename, &my_argc);

  if (result != EXIT_FAILURE) {
    my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
    cmd_line_list_tmp = cmd_line_list;

    for (i = my_argc - 1; i >= 0; --i) {
      my_argv_arg[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

    my_argv_arg[my_argc] = 0;

    additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
    strcpy (additional_error, ADDITIONAL_ERROR);
    strcat (additional_error, filename);
    result =
      cmdline_parser_internal (my_argc, my_argv_arg, args_info,
                              params,
                              additional_error);

    free (additional_error);
    free (my_argv_arg);
  }

  free_cmd_list();
  return result;
}
