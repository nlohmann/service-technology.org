# Configuration file for @PACKAGE_STRING@
# @configure_input@

# This file configures the paths to binaries needed by Marlene. The configure
# script already tried to suggest paths, but its entries might have been left
# empty. In that case, you need to manually add the path to the respective
# tool.

# Marlene needs a Wendy binary (version 1.4 and above). Recent distribtions
# are available at http://service-technology.org/files/wendy.

wendy = @WENDY@

# If you want to cost-optimize the adapters, you have to provide a Candy binary.

candy = @CANDY@

# Providing the binary for Petrify result in nices controller

petrify = @PETRIFY@

# Wendy needs to create a temporary file to pass it to LoLA. The filename
# Wendy should use can be specified below. The filename should end with
# 'XXXXXX' which is replaced at runtime with a random string.


tmpfile = @TEMPFOLDER@

