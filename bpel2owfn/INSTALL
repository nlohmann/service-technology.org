BPEL2oWFN Version 1.0 (16 January 2006)
=======================================

- To compile the excecutable and create the documentation, type `make'.

- To clean up all temporary files, type `make clean'. The documentation and the
  binary is not deleted.

- To delete all files created by building the program, type `make distclean'.

- To check the built excecutable, type `make check'.
