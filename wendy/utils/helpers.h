#ifndef _HELPERS_H
#define _HELPERS_H

#include <vector>

/// updates an index
void next_index(std::vector<unsigned int> &current_index,
                const std::vector<unsigned int> &max_index);

#endif
