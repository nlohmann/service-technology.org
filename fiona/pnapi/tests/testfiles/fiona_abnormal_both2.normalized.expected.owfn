{
  generated by: Fiona 2.0
  input file:   `abnormal_both2.owfn'
  invocation:   `PN API'
  net size:     |P|=23, |P_in|= 4, |P_out|= 4, |T|=10, |F|=36
}

PLACE
  INTERNAL
    p0; p1; p2; a_normalized; b_normalized; x_normalized; x_normalizedCap; y_normalized; y_normalizedCap; c_normalized; d_normalized; z1_normalized; z1_normalizedCap; z2_normalized; z2_normalizedCap;

  INPUT
    a,
    b,
    c,
    d;

  OUTPUT
    x,
    y,
    z1,
    z2;


INITIALMARKING
  p0:	1,
  x_normalizedCap:	1,
  y_normalizedCap:	1,
  z1_normalizedCap:	1,
  z2_normalizedCap:	1;

FINALMARKING
  p2, x_normalizedCap, y_normalizedCap, z1_normalizedCap, z2_normalizedCap;


TRANSITION t24
  CONSUME p0, a_normalized, b_normalized, x_normalizedCap, y_normalizedCap;
  PRODUCE p1, x_normalized, y_normalized;

TRANSITION t25 { input }
  CONSUME a;
  PRODUCE a_normalized;

TRANSITION t26 { input }
  CONSUME b;
  PRODUCE b_normalized;

TRANSITION t27 { output }
  CONSUME x_normalized;
  PRODUCE x, x_normalizedCap;

TRANSITION t28 { output }
  CONSUME y_normalized;
  PRODUCE y, y_normalizedCap;

TRANSITION t29
  CONSUME p1, c_normalized, d_normalized, z1_normalizedCap, z2_normalizedCap;
  PRODUCE p2, z1_normalized, z2_normalized;

TRANSITION t30 { input }
  CONSUME c;
  PRODUCE c_normalized;

TRANSITION t31 { input }
  CONSUME d;
  PRODUCE d_normalized;

TRANSITION t32 { output }
  CONSUME z1_normalized;
  PRODUCE z1, z1_normalizedCap;

TRANSITION t33 { output }
  CONSUME z2_normalized;
  PRODUCE z2, z2_normalizedCap;


{ END OF FILE `abnormal_both2.owfn.owfn' }

