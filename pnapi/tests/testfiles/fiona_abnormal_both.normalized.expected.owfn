{
  generated by: Fiona 2.0
  input file:   `abnormal_both.owfn'
  invocation:   `PN API'
  net size:     |P|=12, |P_in|= 2, |P_out|= 2, |T|=5, |F|=18
}

PLACE
  INTERNAL
    p0; p1; a_normalized; b_normalized; x_normalized; x_normalizedCap; y_normalized; y_normalizedCap;

  INPUT
    a,
    b;

  OUTPUT
    x,
    y;


INITIALMARKING
  p0:	1,
  x_normalizedCap:	1,
  y_normalizedCap:	1;

FINALMARKING
  p1, x_normalizedCap, y_normalizedCap;


TRANSITION t13
  CONSUME p0, a_normalized, b_normalized, x_normalizedCap, y_normalizedCap;
  PRODUCE p1, x_normalized, y_normalized;

TRANSITION t14 { input }
  CONSUME a;
  PRODUCE a_normalized;

TRANSITION t15 { input }
  CONSUME b;
  PRODUCE b_normalized;

TRANSITION t16 { output }
  CONSUME x_normalized;
  PRODUCE x, x_normalizedCap;

TRANSITION t17 { output }
  CONSUME y_normalized;
  PRODUCE y, y_normalizedCap;


{ END OF FILE `abnormal_both.owfn.owfn' }

