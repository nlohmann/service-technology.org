NODES

  0 : (!a * !b),
  1 : (?c + !b),
  2 : (final),
  3 : (!a),
  4 : (!a),
  5 : (?d),
  6 : (final);
  

INITIALNODE
  0;

TRANSITIONS
  0 -> 1 : !a,
  1 -> 2 : ?c,
  1 -> 3 : !b,
  0 -> 4 : !b,
  4 -> 5 : !a,
  5 -> 6 : ?c;
