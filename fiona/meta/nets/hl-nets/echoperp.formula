FORMULA	
ALLPATH 
ALWAYS 
( 
	ALL x : agent : 
		( 
		quiet 
			. 
				(x) 
			< 2
		 
 		AND waiting . (x) < 2 AND terminated . (x) < 2) AND
	    ALL s : session : (uninformed . (s) < 2 AND accepted . (s) < 2)  AND  
		ALL m : message : (pending . (m) < 2  AND mailbox . (m) < 2 ))

	
